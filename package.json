{
  "name": "soltempo",
  "private": true,
  "version": "0.0.0",
  "description": "Tempo merchant treasury — auto-yields idle USDC on Solana via mppsol",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "anchor:build": "anchor build",
    "anchor:test": "anchor test",
    "forge:build": "forge build --root contracts/buffer",
    "forge:test": "forge test --root contracts/buffer"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
